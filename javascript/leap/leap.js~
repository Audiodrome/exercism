var isLeapYear = function() {};

isLeapYear = function(year) {

    // if (year%100 !== 0 && year%4 === 0 || year%400 === 0)
    // 	return true;
    // else
    // 	return false;

    // if (year%4 === 0 || year%400 === 0 && year%100 !== 0)
    // 	return true;
    // else
    // 	return false;

    var isleap = year%100 !== 0 && year%4 === 0 || year%400 === 0 ?
	         true : false;
    
    return isleap;

};

module.exports = isLeapYear;
